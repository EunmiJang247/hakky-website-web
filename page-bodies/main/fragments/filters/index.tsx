import { LoadedLogic } from '../../use-logic';

interface Props {
  logic: LoadedLogic;
}

const FiltersFragment: React.FC<Props> = ({ logic }) => {
  return <div />;
};

export default FiltersFragment;
