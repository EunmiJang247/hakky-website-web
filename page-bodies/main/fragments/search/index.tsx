import { LoadedLogic } from '../../use-logic';

interface Props {
  logic: LoadedLogic;
}

const SearchFragment: React.FC<Props> = ({ logic }) => {
  return <div />;
};

export default SearchFragment;
